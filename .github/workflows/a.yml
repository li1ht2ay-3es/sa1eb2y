on:
  workflow_dispatch:
  schedule:
    - cron: '0 5 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: li1ht2ay-3es/Sa1eB2y@cron
        with:
          token: ${{ secrets.token }}
          auto: true
